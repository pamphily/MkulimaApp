// import {  } from "../utils/majis";

// export function verifyRequest(req: any) {
//     const signature = req.headers["req-signature"];
//     if (!signature) {
//         return { status: 400, message: "Missing Signature" };
//     }

//     if (!verifySignature(req.body, signature)) {
//         return { status: 403, message: "Invalid Signature" };
//     }

//     return null; // Request is valid
// }
