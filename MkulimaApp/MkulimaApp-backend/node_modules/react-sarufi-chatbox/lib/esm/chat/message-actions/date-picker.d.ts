import { CSSProperties } from "react";
interface DatePickerProps {
    selectedDate?: Date;
    onSelect: (date: Date) => void;
    contraints?: {
        minDate?: Date;
        maxDate?: Date;
    };
    styles?: CSSProperties;
}
declare const DatePicker: ({ styles, selectedDate, onSelect, contraints, }: DatePickerProps) => JSX.Element;
export default DatePicker;
