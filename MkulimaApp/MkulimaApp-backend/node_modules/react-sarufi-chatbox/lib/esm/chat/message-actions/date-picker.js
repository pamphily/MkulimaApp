import React, { useState } from "react";
import { Calendar } from "../../assets/icons";
var DatePicker = function (_a) {
    var _b, _c;
    var styles = _a.styles, selectedDate = _a.selectedDate, onSelect = _a.onSelect, contraints = _a.contraints;
    var _d = useState(selectedDate || new Date()), date = _d[0], setDate = _d[1];
    var handleSelect = function (date) {
        setDate(date);
        onSelect(date);
    };
    return (React.createElement("div", { className: "datepicker-toggle", style: styles },
        React.createElement(Calendar, { size: 20, className: "datepicker-toggle-button" }),
        React.createElement("input", { type: "date", className: "datepicker-input", value: date.toISOString().split("T")[0], onChange: function (e) {
                return e.target.value && handleSelect(new Date(e.target.value));
            }, min: (_b = contraints === null || contraints === void 0 ? void 0 : contraints.minDate) === null || _b === void 0 ? void 0 : _b.toISOString().split("T")[0], max: (_c = contraints === null || contraints === void 0 ? void 0 : contraints.maxDate) === null || _c === void 0 ? void 0 : _c.toISOString().split("T")[0] })));
};
export default DatePicker;
