/// <reference types="react" />
declare const VideoPreview: ({ url, caption, fontFamily, fontSize, mediaId, mode, }: {
    url: string;
    caption: string;
    fontFamily: string;
    fontSize: string | number;
    mediaId: string;
    mode?: "dark" | "light" | undefined;
}) => JSX.Element;
export default VideoPreview;
