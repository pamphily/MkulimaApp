import { FC } from "react";
interface Props {
    closeWithBackdrop?: boolean;
    close: () => any;
    children: any;
    open: boolean;
    mode: "dark" | "light";
}
declare const Modal: FC<Props>;
export default Modal;
