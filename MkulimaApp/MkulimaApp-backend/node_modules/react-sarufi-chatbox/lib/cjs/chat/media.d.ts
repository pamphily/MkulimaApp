/// <reference types="react" />
declare const Media: ({ chat, mode, fontFamily, fontSize, messageIndex, }: {
    chat: any;
    mode: "dark" | "light";
    fontFamily: string;
    fontSize: string | number;
    messageIndex: number;
}) => JSX.Element;
export default Media;
