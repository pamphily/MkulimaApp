/// <reference types="react" />
declare const Chatbox: ({ id, open, botId, API_URL, mode, primaryColor, token, fontFamily, fontSize, }: {
    id: number | string;
    botId: string | number;
    API_URL: string;
    open: boolean;
    mode: "light" | "dark";
    primaryColor: string;
    token?: string | undefined;
    fontFamily: string;
    fontSize: string | number;
}) => JSX.Element;
export default Chatbox;
export declare const wrapUrl: (str: string) => string;
export declare const wrap: (str: string) => string;
